import { DynFormElementDirective } from './dyn-form-element.directive';

describe('DynFormElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DynFormElementDirective();
    expect(directive).toBeTruthy();
  });
});
